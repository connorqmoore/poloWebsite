$( document ).ready(function() {
  $( "#a_team" ).click(function( event ) {
    $('#team').ScrollTo();
  });

  $( "#a_roster" ).click(function( event ) {
    $('#roster').ScrollTo();
  });

  $( "#a_practice" ).click(function( event ) {
    $('#practice').ScrollTo();
  });

  $( "#a_contact" ).click(function( event ) {
    $('#contact').ScrollTo();
  });

  $( "#a_calendar" ).click(function( event ) {
    $('#calendar').ScrollTo();
  });

  $( "#calendar_in_team" ).click(function( event ) {
    $('#calendar').ScrollTo();
  });

});
